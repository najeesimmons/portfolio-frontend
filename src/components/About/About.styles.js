import styled, {css} from 'styled-components';

export const Title = styled.h1`
  font-size: 1.5em;
  text-align: center;
  color: green;
  // ${props => props.isBob && css`
  //   background: white;
  //   color: black;
  // `}
`;